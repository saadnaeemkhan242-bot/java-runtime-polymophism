# Java Runtime Polymorphism - Area Calculator

Demonstrates runtime polymorphism in Java using a Shape superclass 
and Circle, Rectangle, Triangle subclasses. Each shape overrides 
calculateArea() called via parent reference at runtime.

## Concepts Covered
- Inheritance
- Method Overriding
- Runtime Polymorphism
- Abstract thinking with parent references
