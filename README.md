# java-runtime-polymophism
Demonstrates runtime polymorphism in Java using a Shape superclass and Circle, Rectangle, Triangle subclasses. Each shape overrides calculateArea() — called via parent reference at runtime.
